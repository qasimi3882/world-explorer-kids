<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose drives the real styling. NoActionBar keeps it fullscreen. -->
    <style name="Theme.WorldExplorerKids" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#4FC3F7</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
